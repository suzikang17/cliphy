# Backlog

Cheap capture — someday/maybe. Promote a line to a real task (`lore promote task <query>`,
the Atlas backlog page, or ask the agent) when it gets an owner, a link, or a place on the board.

- [ ] Add daily bonus claim mechanic for free tier — Free users can claim +1 bonus summary per day by opening the extension. Drives daily engagement and habit formation. Requires explicit claim button vs aut... (was CLIP-61)
- [ ] Show bonus credits in web + mobile UsageBar — Day 65 added `UsageInfo.bonusCredits` and surfaced it in the extension UsageBar. Web (`apps/web/src/components/UsageBar.tsx`) and mobile (`apps/mobile/com... (was CLIP-84)
- [ ] Add podcast and longform content summarization — Expand beyond YouTube to podcasts (RSS/Spotify) and other longform content. Same core pipeline (transcript → AI summary). Broadens value prop from 'YouTub... (was CLIP-62)
- [ ] Audio summaries — listen to summaries on the go — TTS playback of summaries for hands-free consumption (commute, gym). Could use device TTS or server-side with ElevenLabs/OpenAI TTS. Background audio supp... (was CLIP-87)
- [ ] Build out prompt tuning (was CLIP-67)
- [ ] Google Cloud Console — whitelist OAuth callback URL — In Google Cloud Console > APIs & Services > Credentials > OAuth 2.0 Client: add https://cliphy.app/api/auth/google/callback as an authorized redirect URI.... (was CLIP-111)
- [ ] Home screen widget — recent summaries at a glance — iOS home screen widget showing latest summaries or queue status. Tap to jump into app. expo-widgets or react-native-widget-extension. (was CLIP-86)
- [ ] Interest timeline — visualize how your topics evolve — Timeline view showing topic clusters over time. "You went deep on AI in March, shifted to distributed systems in April." Visualization of learning journey... (was CLIP-91)
- [ ] Key moments timeline — visual timestamps in summaries — Extract key moments with timestamps from transcripts during summarization. Visual timeline on summary page — tap a moment to jump to that point in the You... (was CLIP-88)
- [ ] Knowledge graph phase 1 — vector embeddings + semantic search — Enable pgvector in Supabase, embed summaries at save time (during summarization pipeline), store embeddings in summaries table, add "related videos" to su... (was CLIP-82)
- [ ] Knowledge graph phase 2 — entity extraction + topic clusters — Extract entities (people, concepts, tools, frameworks) from summaries via Claude during summarization. Store in edges table (entity ↔ summary). Group summ... (was CLIP-83)
- [ ] Knowledge graph phase 3 — hybrid retrieval + cross-video synthesis — Combine vector similarity + graph traversal for hybrid retrieval. Cross-video synthesis: "here's what your library says about X" by pulling relevant chunk... (was CLIP-84)
- [ ] Notion integration — auto-export summaries to user's Notion — OAuth connect to user's Notion workspace. On summary complete, create a page in their chosen database. Tags as select properties, key moments as toggle bl... (was CLIP-95)
- [ ] Obsidian integration — markdown export to vault — Export summaries as markdown with YAML frontmatter (tags, source URL, date, platform). Could work via Obsidian URI scheme on mobile, or a companion Obsidi... (was CLIP-96)
- [ ] Offline reading — cache summaries for offline access — Cache summaries locally on device so users can read during commute/offline. SQLite or async storage. Sync indicator for stale content. (was CLIP-85)
- [ ] Platform expansion — TikTok, Reels, X videos, Spotify — Add support for additional platforms after podcast support is proven. TikTok, Instagram Reels, Twitter/X videos, Spotify episodes. Each needs a URL parser... (was CLIP-94)
- [ ] Podcast support — transcribe + summarize any podcast episode — Accept podcast RSS feed URL or episode link. Transcript pipeline: 1) Check for <podcast:transcript> tag in RSS → free. 2) Check if episode is on YouTube →... (was CLIP-92)
- [ ] Product Hunt launch (was CLIP-31)
- [ ] Queue videos for specific topic (was CLIP-64)
- [ ] Quick gist mode — 10-second summary for fast decisions — Ultra-short 1-2 sentence "gist" generated alongside the full summary. Shown as preview in queue list. Helps users decide whether to watch the full video o... (was CLIP-89)
- [ ] Readwise integration — send summaries as highlights — Push summaries and key moments to Readwise via their API. Users see Cliphy summaries alongside their book highlights and article saves. Simple API integra... (was CLIP-97)
- [ ] Record demo GIF/video (was CLIP-26)
- [ ] Reddit / Indie Hackers / Twitter launch posts (was CLIP-32)
- [ ] Select sections to copy rather than whole thing (was CLIP-63)
- [ ] Set up domain + DNS (was CLIP-34)
- [ ] Set up Vercel env vars for auto-subscriptions — Add to Vercel (Production + Preview + Development): YOUTUBE_API_KEY, GOOGLE_CLIENT_ID, GOOGLE_CLIENT_SECRET, GOOGLE_REDIRECT_URI (https://cliphy.app/api/a... (was CLIP-110)
- [ ] Templates can choose sections to include or not (was CLIP-65)
- [ ] Video chat — conversational Q&A about video content — Chat interface on summary page — ask follow-up questions about the video. "What did they say about scaling?" "Explain the part about caching." Uses transc... (was CLIP-93)
- [ ] Webhook API — on-summary-complete event for custom integrations — Fire a webhook when a summary completes. Payload includes summary, tags, key moments, source URL. Lets power users wire up Zapier, Make, Logseq, Roam, App... (was CLIP-98)
- [ ] Weekly digest — smart notifications about your content — Push notification or in-app digest: "You saved 5 videos about system design this week — here’s a synthesis." Surfaces patterns in consumption. Drives re-e... (was CLIP-90)
- [ ] (was CLIP-80)
- [ ] Full multi-tweet / self-thread capture — free syndication + FixTweet endpoints only return the single root tweet (no forward thread children). Revisit via screenshot-OCR (subproject #2) or a paid path. See ADR 0043.
